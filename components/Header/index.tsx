import React from 'react';
import styles from './Header.module.scss';
import clsx from 'clsx';

function Header() {
  return <header className={clsx(styles.song)}>Header</header>;
}
export default Header;
